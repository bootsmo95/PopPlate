---
phase: 03-ui-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/platform/r/[slug]/index.vue
  - app/pages/platform/r/[slug]/dishes.vue
autonomous: true
requirements:
  - UIPOL-03
  - UIPOL-04

must_haves:
  truths:
    - "Restaurant workspace page shows structured p-card empty state with icon, heading, body, and CTA when no dishes exist"
    - "Restaurant dishes page shows structured p-card empty state with icon, heading, body, and CTA when no dishes linked"
    - "Empty state CTAs navigate to actionable next step (add dish / view all dishes)"
    - "Both empty states are usable on mobile without overflow"
  artifacts:
    - path: "app/pages/platform/r/[slug]/index.vue"
      provides: "Restaurant workspace with full p-card empty state"
      contains: "Ingen retter endnu"
    - path: "app/pages/platform/r/[slug]/dishes.vue"
      provides: "Restaurant dishes with full p-card empty state"
      contains: "Ingen retter på denne menu"
  key_links:
    - from: "app/pages/platform/r/[slug]/index.vue"
      to: "/platform/dishes/new"
      via: "NuxtLink CTA in empty state"
      pattern: "to=\"/platform/dishes/new\""
    - from: "app/pages/platform/r/[slug]/dishes.vue"
      to: "/platform/dishes"
      via: "NuxtLink CTA in empty state"
      pattern: "to=\"/platform/dishes\""
---

<objective>
Upgrade the two remaining empty state gaps (restaurant workspace and restaurant dishes) from minimal text-only divs to full structured p-card empty states with icon, heading, body copy, and CTA button matching the established pattern.

Purpose: Users who land on a restaurant page with no dishes see guidance toward their next action (UIPOL-03). Both states use existing responsive patterns ensuring mobile usability (UIPOL-04).
Output: 2 modified page files with upgraded empty states.
</objective>

<execution_context>
@.planning/phases/03-ui-polish/03-UI-SPEC.md
@.planning/phases/03-ui-polish/03-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
Empty state pattern from app/pages/platform/restaurants.vue (lines 174-180):
```html
<div v-if="!restaurants?.length" class="p-card py-16 text-center">
  <h3 class="font-display font-normal text-[22px] tracking-[-0.015em] mb-2">Ingen restauranter endnu</h3>
  <p class="text-ink-mute text-[15px] mb-6">Opret din foerste restaurant for at komme i gang.</p>
  <button type="button" class="top-btn top-btn--primary" @click="showCreate = true">
    <Icon name="plus" :size="14" /><span>Ny restaurant</span>
  </button>
</div>
```

Icon component usage:
```html
<Icon name="dish" :size="32" class="text-ink-faint mx-auto mb-3" />
```

NuxtLink pattern:
```html
<NuxtLink to="/platform/dishes/new" class="top-btn top-btn--primary !inline-flex">
  <Icon name="plus" :size="14" /><span>Tilfoej ret</span>
</NuxtLink>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade empty states in r/[slug]/index.vue and r/[slug]/dishes.vue</name>
  <files>app/pages/platform/r/[slug]/index.vue, app/pages/platform/r/[slug]/dishes.vue</files>
  <read_first>
    - app/pages/platform/r/[slug]/index.vue (full file — find the existing v-if="!dishes.length" block around line 169 to replace)
    - app/pages/platform/r/[slug]/dishes.vue (full file — find the existing v-if="!dishes.length" block around line 113 to replace)
    - app/pages/platform/restaurants.vue (lines 170-185 — reference empty state pattern with p-card, font-display heading, ink-mute body, top-btn CTA)
    - app/components/shared/Icon.vue (verify "dish" and "plus" icon names exist)
  </read_first>
  <action>
**Modify `app/pages/platform/r/[slug]/index.vue`:**

Find the existing minimal empty state block (around line 169):
```html
<div v-if="!dishes.length" class="py-12 text-center text-ink-faint">
  Ingen retter endnu. Opret den foerste for at komme i gang.
</div>
```

Replace it entirely with the structured p-card empty state:
```html
<div v-if="!dishes.length" class="p-card py-16 text-center">
  <Icon name="dish" :size="32" class="text-ink-faint mx-auto mb-3" />
  <h3 class="font-display font-normal text-[18px] tracking-[-0.015em] mb-2">Ingen retter endnu</h3>
  <p class="text-ink-mute text-[13px] mb-5">Tilføj din første ret for at komme i gang.</p>
  <NuxtLink to="/platform/dishes/new" class="top-btn top-btn--primary !inline-flex">
    <Icon name="plus" :size="14" /><span>Tilføj ret</span>
  </NuxtLink>
</div>
```

Ensure Icon is imported at the top of the script section if not already auto-imported. Check if the file already imports Icon — if so, no change needed. If not, add:
```typescript
import Icon from '~/components/shared/Icon.vue'
```

Do NOT modify any other parts of the file — the `v-else-if` branch for filtered/search results and the main dish list rendering stay unchanged.

**Modify `app/pages/platform/r/[slug]/dishes.vue`:**

Find the existing minimal empty state block (around line 113):
```html
<div v-if="!dishes.length" class="p-card py-16 text-center text-ink-faint">
  <div class="font-display italic text-2xl text-ink mb-2">Ingen retter endnu</div>
  <p>Opret en ret fra denne restaurant for at tilknytte den korrekt.</p>
</div>
```

Replace it entirely with the structured p-card empty state:
```html
<div v-if="!dishes.length" class="p-card py-16 text-center">
  <Icon name="dish" :size="32" class="text-ink-faint mx-auto mb-3" />
  <h3 class="font-display font-normal text-[18px] tracking-[-0.015em] mb-2">Ingen retter på denne menu</h3>
  <p class="text-ink-mute text-[13px] mb-5">Retter vises her, når du tilknytter dem til restauranten.</p>
  <NuxtLink to="/platform/dishes" class="top-btn !inline-flex">
    <span>Se alle retter</span>
  </NuxtLink>
</div>
```

Note: The CTA uses ghost variant (`top-btn` without `--primary`) because the action is navigational (viewing all dishes), not a primary creation action. This matches the UI-SPEC copywriting contract.

Ensure Icon import exists. Do NOT modify other parts of the file.
  </action>
  <verify>
    <automated>cd C:/repos/PopPlate && npx nuxi typecheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/pages/platform/r/[slug]/index.vue contains `class="p-card py-16 text-center"` (not the old `py-12 text-center text-ink-faint`)
    - app/pages/platform/r/[slug]/index.vue contains `Icon name="dish" :size="32"`
    - app/pages/platform/r/[slug]/index.vue contains `Ingen retter endnu` as h3 heading text
    - app/pages/platform/r/[slug]/index.vue contains `Tilføj din første ret for at komme i gang.` as body text
    - app/pages/platform/r/[slug]/index.vue contains `to="/platform/dishes/new"` CTA link
    - app/pages/platform/r/[slug]/index.vue contains `Tilføj ret` CTA text
    - app/pages/platform/r/[slug]/index.vue contains `font-display font-normal text-[18px]` heading style
    - app/pages/platform/r/[slug]/dishes.vue contains `class="p-card py-16 text-center"` (without text-ink-faint)
    - app/pages/platform/r/[slug]/dishes.vue contains `Icon name="dish" :size="32"`
    - app/pages/platform/r/[slug]/dishes.vue contains `Ingen retter på denne menu` as h3 heading text
    - app/pages/platform/r/[slug]/dishes.vue contains `Retter vises her, når du tilknytter dem til restauranten.` as body text
    - app/pages/platform/r/[slug]/dishes.vue contains `to="/platform/dishes"` CTA link
    - app/pages/platform/r/[slug]/dishes.vue contains `Se alle retter` CTA text
    - app/pages/platform/r/[slug]/dishes.vue does NOT contain `font-display italic text-2xl` (old style removed)
  </acceptance_criteria>
  <done>Both restaurant workspace and restaurant dishes pages show full structured p-card empty states with dish icon, Danish heading, descriptive body, and actionable CTA button. Empty states use responsive p-card pattern that works on mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Template-only changes — no data flow modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Spoofing | NuxtLink CTA | accept | Links point to internal platform routes only (`/platform/dishes/new`, `/platform/dishes`). No user-controlled URLs. No risk. |
</threat_model>

<verification>
- `npx nuxi typecheck` passes
- Navigate to a restaurant workspace with no dishes — p-card empty state with "Tilføj ret" CTA visible
- Navigate to restaurant dishes tab with no linked dishes — p-card empty state with "Se alle retter" CTA visible
- Resize browser to 375px width — empty states stack correctly, no horizontal overflow
</verification>

<success_criteria>
Both empty state gaps filled with structured p-card pattern matching the established design. CTAs link to correct next-action pages. Mobile responsive without horizontal scroll.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-polish/03-03-SUMMARY.md`
</output>
