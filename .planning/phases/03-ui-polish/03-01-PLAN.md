---
phase: 03-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/composables/useToast.ts
  - app/components/platform/ToastContainer.vue
  - app/components/shared/ActionButton.vue
  - app/error.vue
  - app/layouts/platform.vue
autonomous: true
requirements:
  - UIPOL-01
  - UIPOL-02

must_haves:
  truths:
    - "useToast() composable exposes toast.success, toast.error, toast.undo methods and a reactive toasts array"
    - "ToastContainer renders toasts fixed bottom-right with slide-up animation and auto-dismiss"
    - "ActionButton shows spinner and disables when loading=true, maps variant to correct CSS class"
    - "error.vue renders a branded Danish error page for any unhandled error"
    - "ToastContainer is mounted in platform layout"
  artifacts:
    - path: "app/composables/useToast.ts"
      provides: "Toast state management composable"
      exports: ["useToast"]
    - path: "app/components/platform/ToastContainer.vue"
      provides: "Fixed-position toast notification renderer"
    - path: "app/components/shared/ActionButton.vue"
      provides: "Reusable loading-aware button component"
    - path: "app/error.vue"
      provides: "Global Nuxt error page"
    - path: "app/layouts/platform.vue"
      provides: "ToastContainer mount point"
  key_links:
    - from: "app/components/platform/ToastContainer.vue"
      to: "app/composables/useToast.ts"
      via: "useToast() composable import"
      pattern: "useToast\\(\\)"
    - from: "app/layouts/platform.vue"
      to: "app/components/platform/ToastContainer.vue"
      via: "component mount in template"
      pattern: "<ToastContainer"
---

<objective>
Create the four foundational UI polish components: useToast composable, ToastContainer, ActionButton, and error.vue. Mount ToastContainer in the platform layout.

Purpose: These are the shared building blocks that Plan 02 wires into every platform page. Without them, no page can show toast feedback or loading buttons.
Output: 4 new files + 1 modified layout, all independently testable.
</objective>

<execution_context>
@.planning/phases/03-ui-polish/03-UI-SPEC.md
@.planning/phases/03-ui-polish/03-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From app/composables/useAuth.ts (composable structure pattern):
```typescript
// Named export function pattern
export function useAuth() {
  // useState for SSR-safe shared state
  // action functions scoped inside
  // single return object
  return { user, isAuthenticated, login, logout }
}
```

From app/components/shared/Icon.vue (shared component props pattern):
```typescript
const props = withDefaults(
  defineProps<{
    name: 'home' | 'dish' | ...
    size?: number | string
    stroke?: number | string
  }>(),
  { size: 16, stroke: 1.4 },
)
```

From app/layouts/platform.vue (layout template — lines 48-66):
```html
<template>
  <div class="platform-shell grid min-h-screen" style="grid-template-columns: 260px 1fr;">
    <Sidebar ... />
    <main class="platform-main p-9 px-12 pb-20 ...">
      <slot />
    </main>
  </div>
</template>
```

From app/assets/css/main.css (button classes):
```css
.top-btn { @apply inline-flex items-center gap-2 h-9 px-4 rounded-full
           border border-line-strong text-[13px] font-medium text-ink ...; }
.top-btn--primary { @apply bg-ink text-ink-inv border-ink; }
.top-btn--primary:hover { @apply bg-clay-deep border-clay-deep; }
```

Transition pattern from app/pages/platform/restaurants.vue:
```vue
<style scoped>
.slide-enter-active { transition: all 200ms ease-out; }
.slide-leave-active { transition: all 150ms ease-in; }
.slide-enter-from, .slide-leave-to { opacity: 0; transform: translateY(-8px); }
</style>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useToast composable, ToastContainer component, and mount in platform layout</name>
  <files>app/composables/useToast.ts, app/components/platform/ToastContainer.vue, app/layouts/platform.vue</files>
  <read_first>
    - app/composables/useAuth.ts (composable structure pattern — named export, useState for SSR safety)
    - app/components/platform/PageSkeleton.vue (SFC structure pattern — script setup, scoped styles)
    - app/layouts/platform.vue (current template to modify — mount point for ToastContainer)
    - app/pages/platform/restaurants.vue (transition animation pattern — slide-enter-active/leave-active)
    - app/assets/css/main.css (existing token classes — p-card, bg-paper, border-line, text-ink)
    - app/components/shared/Icon.vue (Icon component usage — name="close" prop)
  </read_first>
  <action>
**1. Create `app/composables/useToast.ts`:**

Define the Toast interface and composable:

```typescript
interface Toast {
  id: string
  type: 'success' | 'error' | 'undo'
  message: string
  onUndo?: () => void
  createdAt: number
}

export function useToast() {
  const toasts = useState<Toast[]>('toasts', () => [])

  function dismiss(id: string) {
    toasts.value = toasts.value.filter(t => t.id !== id)
  }

  function addToast(toast: Omit<Toast, 'id' | 'createdAt'>, autoDismissMs?: number) {
    const id = Date.now().toString()
    const entry: Toast = { ...toast, id, createdAt: Date.now() }
    // Prepend new toast, cap at 3 visible
    toasts.value = [entry, ...toasts.value].slice(0, 3)
    if (autoDismissMs) {
      setTimeout(() => dismiss(id), autoDismissMs)
    }
  }

  const toast = {
    success(message: string) { addToast({ type: 'success', message }, 3500) },
    error(message: string) { addToast({ type: 'error', message }) },
    undo(message: string, options: { onUndo: () => void }) {
      addToast({ type: 'undo', message, onUndo: options.onUndo }, 5000)
    },
  }

  return { toasts: readonly(toasts), toast, dismiss }
}
```

Key details: Use `useState` (not `ref`) for SSR-safe shared state across layout and pages. Named export `useToast` (not default). Auto-dismiss: 3500ms for success, 5000ms for undo, none for error (persist until dismissed). Max 3 toasts — oldest trimmed on overflow.

**2. Create `app/components/platform/ToastContainer.vue`:**

```vue
<script setup lang="ts">
import Icon from '~/components/shared/Icon.vue'

const { toasts, dismiss } = useToast()
</script>
```

Template structure:
- Outer wrapper: `<div class="fixed bottom-8 right-8 z-50 flex flex-col-reverse gap-6 max-[480px]:bottom-6 max-[480px]:right-6">`
- Use `<TransitionGroup name="toast">` wrapping the toast items
- Each toast item: `<div v-for="t in toasts" :key="t.id" class="w-[320px] max-w-[calc(100vw-48px)] bg-paper border border-line rounded-md shadow-[0_14px_30px_rgba(26,20,16,0.10)] px-4 py-4 flex items-start gap-3" :class="{ 'border-l-[3px] border-l-clay': t.type === 'success', 'border-l-[3px] border-l-[#a85a48]': t.type === 'error' || t.type === 'undo' }">`
- Message text: `<span class="text-[14px] text-ink leading-[1.4] flex-1">{{ t.message }}</span>`
- Undo link (shown only for type 'undo'): `<button v-if="t.type === 'undo' && t.onUndo" @click="t.onUndo(); dismiss(t.id)" class="text-[13px] font-medium text-clay-deep underline-offset-2 hover:underline shrink-0">Fortryd</button>`
- Dismiss button (all toasts): `<button @click="dismiss(t.id)" class="text-ink-mute hover:text-ink shrink-0 mt-0.5" aria-label="Luk besked"><Icon name="close" :size="14" /></button>`

Scoped styles for TransitionGroup animation:
```css
.toast-enter-active { transition: all 200ms ease-out; }
.toast-leave-active { transition: all 150ms ease-in; }
.toast-enter-from { opacity: 0; transform: translateY(8px); }
.toast-leave-to { opacity: 0; }
```

Note: `prefers-reduced-motion` is handled globally by main.css — no per-component override needed.

**3. Modify `app/layouts/platform.vue`:**

Add import at top of script setup (after existing Sidebar import on line 2):
```typescript
import ToastContainer from '~/components/platform/ToastContainer.vue'
```

Add `<ToastContainer />` in template as last child inside the `.platform-shell` div, AFTER the `</main>` closing tag but BEFORE the closing `</div>` on line 65. This keeps it outside the grid flow as a fixed-position element.

The template should become:
```html
<template>
  <div class="platform-shell grid min-h-screen" style="grid-template-columns: 260px 1fr;">
    <Sidebar ... />
    <main ...>
      <slot />
    </main>
    <ToastContainer />
  </div>
</template>
```
  </action>
  <verify>
    <automated>cd C:/repos/PopPlate && npx nuxi typecheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/composables/useToast.ts contains `export function useToast()`
    - app/composables/useToast.ts contains `useState<Toast[]>('toasts'`
    - app/composables/useToast.ts contains `success(message: string)` and `error(message: string)` and `undo(message: string`
    - app/composables/useToast.ts contains `setTimeout(() => dismiss(id), 3500)` for success auto-dismiss
    - app/composables/useToast.ts contains `setTimeout(() => dismiss(id), 5000)` for undo auto-dismiss
    - app/composables/useToast.ts contains `.slice(0, 3)` for max 3 toast cap
    - app/components/platform/ToastContainer.vue contains `fixed bottom-8 right-8 z-50`
    - app/components/platform/ToastContainer.vue contains `max-[480px]:bottom-6 max-[480px]:right-6`
    - app/components/platform/ToastContainer.vue contains `w-[320px] max-w-[calc(100vw-48px)]`
    - app/components/platform/ToastContainer.vue contains `border-l-[3px] border-l-clay` for success stripe
    - app/components/platform/ToastContainer.vue contains `border-l-[3px] border-l-[#a85a48]` for error/undo stripe
    - app/components/platform/ToastContainer.vue contains `aria-label="Luk besked"` on dismiss button
    - app/components/platform/ToastContainer.vue contains `Fortryd` undo link text
    - app/components/platform/ToastContainer.vue contains `TransitionGroup name="toast"`
    - app/components/platform/ToastContainer.vue contains `.toast-enter-from { opacity: 0; transform: translateY(8px); }`
    - app/layouts/platform.vue contains `import ToastContainer from '~/components/platform/ToastContainer.vue'`
    - app/layouts/platform.vue contains `<ToastContainer />`
  </acceptance_criteria>
  <done>useToast composable provides toast.success/error/undo methods with auto-dismiss timers. ToastContainer renders fixed bottom-right with slide-up animation, 3px left accent stripe per type, dismiss button, and undo link. Platform layout mounts ToastContainer.</done>
</task>

<task type="auto">
  <name>Task 2: Create ActionButton component and error.vue page</name>
  <files>app/components/shared/ActionButton.vue, app/error.vue</files>
  <read_first>
    - app/components/shared/Icon.vue (shared component pattern — withDefaults, defineProps)
    - app/components/shared/LogoMark.vue (logo component for error page)
    - app/assets/css/main.css (top-btn, top-btn--primary classes, font-display, text-ink-mute, bg-bg tokens)
    - app/pages/platform/dishes/[id].vue (current button loading pattern to understand what ActionButton replaces)
  </read_first>
  <action>
**1. Create `app/components/shared/ActionButton.vue`:**

```vue
<script setup lang="ts">
const props = withDefaults(
  defineProps<{
    loading?: boolean
    disabled?: boolean
    variant?: 'primary' | 'ghost' | 'danger'
    type?: 'button' | 'submit'
    size?: 'md' | 'sm'
  }>(),
  {
    loading: false,
    disabled: false,
    variant: 'ghost',
    type: 'button',
    size: 'md',
  },
)

const variantClass = computed(() => {
  switch (props.variant) {
    case 'primary': return 'top-btn top-btn--primary'
    case 'danger': return 'top-btn text-[#a85a48] border-[rgba(168,90,72,0.30)] hover:bg-[rgba(168,90,72,0.06)]'
    default: return 'top-btn'
  }
})

const sizeClass = computed(() => {
  return props.size === 'sm' ? 'min-w-[80px]' : 'min-w-[100px]'
})
</script>
```

Template:
```html
<template>
  <button
    :type="type"
    :disabled="loading || disabled"
    :class="[
      variantClass,
      sizeClass,
      {
        'opacity-70 cursor-not-allowed': loading,
        'opacity-50 cursor-not-allowed': disabled && !loading,
      }
    ]"
  >
    <template v-if="loading">
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none"
           class="animate-[spin_0.7s_linear_infinite] shrink-0 mx-auto"
           aria-hidden="true">
        <circle cx="7" cy="7" r="5" stroke="currentColor" stroke-width="1.6"
                stroke-dasharray="22" stroke-dashoffset="8" stroke-linecap="round"
                opacity="0.4" />
        <path d="M7 2a5 5 0 0 1 5 5" stroke="currentColor" stroke-width="1.6"
              stroke-linecap="round" />
      </svg>
    </template>
    <template v-else>
      <slot />
    </template>
  </button>
</template>
```

No scoped styles needed — relies entirely on existing `top-btn` classes from main.css. The `transition: opacity 150ms ease` is already handled by top-btn base styles.

**2. Create `app/error.vue`:**

Nuxt error page — receives `error` prop automatically. Single layout for all error codes per D-09.

```vue
<script setup lang="ts">
import LogoMark from '~/components/shared/LogoMark.vue'

useHead({ title: 'Fejl \u00b7 popplate' })

const props = defineProps<{
  error: { statusCode?: number; statusMessage?: string; message?: string }
}>()

const handleBack = () => clearError({ redirect: '/' })
</script>
```

Template:
```html
<template>
  <div class="min-h-screen flex items-center justify-center bg-bg">
    <div class="text-center max-w-[480px] px-6">
      <LogoMark :size="32" class="mx-auto mb-12" />
      <div
        v-if="error?.statusCode"
        class="font-mono text-[11px] uppercase font-medium text-ink-faint tracking-[0.22em] mb-4"
      >
        {{ error.statusCode }}
      </div>
      <h1 class="font-display font-normal text-[32px] tracking-[-0.025em] text-ink mb-3">
        Noget gik galt
      </h1>
      <p class="text-[15px] text-ink-mute leading-[1.5] mb-8">
        Siden kunne ikke indlæses. Prøv igen, eller gå tilbage til forsiden.
      </p>
      <button class="top-btn top-btn--primary" @click="handleBack">
        Tilbage til forsiden
      </button>
    </div>
  </div>
</template>
```

No `definePageMeta` needed — Nuxt error.vue is automatically layoutless. Uses `clearError({ redirect: '/' })` for navigation (Nuxt standard API).
  </action>
  <verify>
    <automated>cd C:/repos/PopPlate && npx nuxi typecheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/components/shared/ActionButton.vue contains `defineProps<{` with `loading?: boolean`
    - app/components/shared/ActionButton.vue contains `variant?: 'primary' | 'ghost' | 'danger'`
    - app/components/shared/ActionButton.vue contains `top-btn top-btn--primary` in variantClass computed
    - app/components/shared/ActionButton.vue contains `text-[#a85a48] border-[rgba(168,90,72,0.30)]` for danger variant
    - app/components/shared/ActionButton.vue contains `animate-[spin_0.7s_linear_infinite]` spinner SVG
    - app/components/shared/ActionButton.vue contains `opacity-70 cursor-not-allowed` for loading state
    - app/components/shared/ActionButton.vue contains `opacity-50 cursor-not-allowed` for disabled state
    - app/components/shared/ActionButton.vue contains `:disabled="loading || disabled"`
    - app/components/shared/ActionButton.vue contains `v-if="loading"` to toggle spinner vs slot
    - app/error.vue contains `clearError({ redirect: '/' })`
    - app/error.vue contains `Noget gik galt` heading
    - app/error.vue contains `Siden kunne ikke indlæses` body text
    - app/error.vue contains `Tilbage til forsiden` CTA text
    - app/error.vue contains `font-mono text-[11px] uppercase` error code eyebrow
    - app/error.vue contains `Fejl` in useHead title
    - app/error.vue contains `LogoMark :size="32"`
    - app/error.vue contains `min-h-screen flex items-center justify-center bg-bg`
  </acceptance_criteria>
  <done>ActionButton component handles primary/ghost/danger variants with spinner-replaces-text loading state. Error page renders branded Danish page with logo, status code eyebrow, heading, body, and CTA button for all error codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-only | All new components are client-side presentation only; no data crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | error.vue | mitigate | Do not render error.message in the page body — only show statusCode (numeric) and fixed Danish copy. Raw error details stay in console only. |
| T-03-02 | Denial of Service | useToast | accept | Toast cap at 3 prevents DOM flooding. Auto-dismiss timers prevent memory accumulation. Low risk — client-only state. |
</threat_model>

<verification>
- `npx nuxi typecheck` passes with no errors
- Open any platform page in browser — ToastContainer div exists in DOM (fixed positioned, empty until triggered)
- Manually test: in browser console, call composable to verify toast appears (dev tools)
</verification>

<success_criteria>
All four new files created and type-checking. ToastContainer mounted in platform layout. ActionButton renders three variants with loading spinner. Error page shows branded Danish content for any error code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-polish/03-01-SUMMARY.md`
</output>
